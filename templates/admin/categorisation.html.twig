{% extends 'base_admin.html.twig' %}

{% block body %}
    <h1>{{ reponse.questions.ordre }}) - {{ reponse.questions.question }}</h1>
    <p>{{ reponse.valeur }}</p>
    {{ form(form) }}
{% endblock %}